name: Build and Push Speedybot
on:
  push:
    branches: [ main ]
    paths:
      - 'Dockerfile'

jobs:
  build-and-push:
    runs-on: vulcan
    steps:
      - name: Checkout code
        uses: actions/checkout@b82d40a8588b2de483fd868330be7bc83ef534b1aa2653d1de4770087400e193

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@c6a4d59835e2619a6ab9a20b39020171bf4c24116e2ebb3ddf2337d3f09a80eb

      # REMOVED: Login to Private Registry (Not needed for open internal registry)

      - name: Cleanup Remote Manifest
        run: |
          echo "Checking for existing :latest manifest..."
          DIGEST=$(curl -sI -k \
            -H "Accept: application/vnd.docker.distribution.manifest.v2+json, application/vnd.oci.image.manifest.v1+json" \
            "https://reg.h.koalaheim.com/v2/speedybot/manifests/latest" | \
            grep -iE "^Docker-Content-Digest:" | awk '{print $2}' | tr -d '\r\n')
          
          if [ ! -z "$DIGEST" ]; then
            echo "Found Digest: $DIGEST. Deleting old manifest for GC..."
            curl -s -k -X DELETE "https://reg.h.koalaheim.com/v2/speedybot/manifests/$DIGEST"
          else
            echo "No manifest found, proceeding with build."
          fi

      - name: Build and Push AMD64
        uses: docker/build-push-action@30bac95c4dc30b045496e4978717c8aa4d3bba949ac5574bb718e39d6dbeeae1
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          provenance: false
          tags: |
            reg.h.koalaheim.com/speedybot:latest
